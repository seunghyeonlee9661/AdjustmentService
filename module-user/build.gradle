dependencies {
    implementation project(':module-common')
}

springBoot{
    mainClass.set('com.sparta.UserApplication') // 여기에 실제 메인 클래스의 경로를 지정
}

tasks.named('bootJar') {
    enabled = false
}

tasks.named('jar') {
    enabled = true
}