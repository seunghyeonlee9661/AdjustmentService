plugins {
}

dependencies {
    // common 모듈 추가
    implementation project(':common')
}

bootJar {
    mainClass.set('com.sparta.adjust.AdjustServiceApplication') // 변경된 설정
    archiveFileName.set("adjustment-${version}.jar")
}