dependencies {
    implementation project(':module-common')
}

springBoot{
    mainClass.set('com.sparta.VideoApplication') // 여기에 실제 메인 클래스의 경로를 지정
}

bootJar {
    enabled = false
}
jar {
    enabled = true
}